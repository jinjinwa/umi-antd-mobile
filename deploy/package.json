{
  "name": "umi_mall_publish",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "pro": "npm install && pm2 startOrRestart ecosystem.config.js --env production"
  },
  "author": "superman",
  "license": "ISC",
  "dependencies": {
    "connect-history-api-fallback": "^1.3.0",
    "express": "^4.14.0",
    "path": "^0.12.7"
  }
}
